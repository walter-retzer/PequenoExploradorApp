package com.example.pequenoexploradorapp.presentation.components.parallax.model

import androidx.compose.ui.Alignment

data class ContainerSettings(
    val scale: Float = 1f,
    val alignment: Alignment = Alignment.Center,
)